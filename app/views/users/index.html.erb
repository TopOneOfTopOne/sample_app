<%= provide(:title, "All Users") %>

<ul class="users">
  <%= will_paginate %>
  <% @users.each do |user| %>
    <li>
      <a href="<%=user_url(user)%>"><%= gravatar_for(user, size: 50) %></a>
      <%= link_to user.name, user %>
    </li>
  <% end %>
  <%= will_paginate %>
</ul>