<%= provide(:title, "All Users") %>

<ul class="users">
  <%= will_paginate %>
  <%= render @users %> <!-- rails auto goes through each user and calls the _user partial -->
  <%= will_paginate %>
</ul>